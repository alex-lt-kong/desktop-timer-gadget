using MamsdsTimer.Properties;
using System;
using System.Collections.Generic;
using System.Configuration;
using System.Data;
using System.IO;
using System.Linq;
using System.Windows;
using System.Windows.Markup;

namespace MamsdsTimer
{
    /// <summary>
    /// App.xaml 的交互逻辑
    /// </summary>
    public partial class App : Application
    {
    /*    private void Application_Startup(object sender, StartupEventArgs e)
        {

            MainWindow MyWindow = (MainWindow)Settings.Default.WindowState;
            MyWindow.Show();
        } 
     /*   public App()
        {
            MainWindow MyWindow = new MainWindow();
      //      FileStream MyStream = new FileStream(@"E:\Hello.dat", FileMode.Open);
      //      MyWindow = (MainWindow)XamlReader.Load(MyStream);
            MyWindow.Show();
      //      MyStream.Dispose(); 
        }*/
    }
}
